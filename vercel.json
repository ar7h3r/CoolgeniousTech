{
  "buildCommand": "cd CoolgeniousTech && bundle install && bundle exec jekyll build",
  "outputDirectory": "CoolgeniousTech/_site",
  "installCommand": "echo 'dependencies handled in buildCommand'"
}
